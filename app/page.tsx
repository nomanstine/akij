import { LoginForm } from '../components/login-form';

export default function Login() {
  return(
    <main className="h-full flex items-center justify-center bg-[#F9FAFB] px-4 py-8">
      <LoginForm />
    </main>
  ) 
    
}